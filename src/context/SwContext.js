import createDataContext from './createDataContext';

const familyReducer = (state, action) => {
    switch (action.type) {
        case 'add_family':
            //TODO: 
            return null;
        
    }
};

const addFamily = (dispatch) =>{
    try {

    } catch (err) {

    };
};